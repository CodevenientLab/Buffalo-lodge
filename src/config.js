// Supply the lodge-owned GA4 measurement ID only after updating the privacy notice.
// No analytics loads with this value empty, or before optional consent.
window.LODGE_CONFIG = Object.freeze({ ga4MeasurementId: '' });
